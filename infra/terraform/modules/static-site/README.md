# static-site module

Creates a private S3 bucket, CloudFront distribution with Origin Access Control, optional ACM certificate, and optional Route 53 records.

ACM certificates for CloudFront are created in `us-east-1`.
